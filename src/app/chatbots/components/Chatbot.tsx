"use client";
import { useMutation, useQuery } from "@blitzjs/rpc";
import Link from "next/link";
import { useRouter } from "next/navigation";


export const Chatbot = ({ chatbotId }: { chatbotId: number }) => {
  const router = useRouter();
  
  return (
    <>
      <div>
      
      </div>
    </>
  );
};
